import React from "react";

const Title = ({ title }) => {
  return (
    <>
      <h1 className="md:text-[50px] text-[#fb8569] text-3xl md:pt-[50px] pb-0 text-center font-bold ">{title}</h1>
    </>
  );
};

export default Title;
